spring.application.name=IntelliSpace
spring.datasource.url=${db_url}
spring.datasource.driver-class-name=org.postgresql.Driver
spring.jpa.hibernate.ddl-auto=update
spring.datasource.hikari.maximum-pool-size=20
spring.datasource.hikari.minimum-idle=5

server.port=8888
server.servlet.context-path=/api

spring.servlet.multipart.max-file-size=100MB
spring.servlet.multipart.max-request-size=100MB

spring.security.oauth2.client.registration.google.client-id=${GOOGLE_CLIENT_ID}
spring.security.oauth2.client.registration.google.client-secret=${GOOGLE_CLIENT_SECRET}

connection_string=${azureString}
spring.security.oauth2.client.registration.github.client-id=${GITHUB_CLIENT_ID}
spring.security.oauth2.client.registration.github.client-secret=${GITHUB_CLIENT_SECRET}

openAIKey=${OPEN_AI_API_KEY}
openAIModel=${OPEN_AI_MODEL}
openAIURL=${OPEN_AI_URL}

spring.mail.host=smtp.gmail.com
spring.mail.port=587
spring.mail.username=intellispace.contact@gmail.com
spring.mail.password=atywtippjhrlnwkt
spring.mail.properties.mail.smtp.auth= true
spring.mail.properties.mail.smtp.starttls.enable= true

logging.level.org.springframework.web.socket=DEBUG